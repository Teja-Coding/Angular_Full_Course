let UserInfo:{userName:string, userSal:number,userAddres:string[] }={
    userName:"manikanta",
    userSal:55000,
    userAddres:[
        "tanuku",
        "penumantra"
    ]
}

console.log(`name of the user is ${UserInfo.userName} and current salary is ${UserInfo.userSal} and address is ${UserInfo.userAddres[1]}`)